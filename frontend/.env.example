# Social login (OAuth 2.0). Copy to .env and paste your keys — never commit real keys.
# Vite exposes env vars via import.meta.env; only the public identifiers live here.
VITE_GOOGLE_CLIENT_ID=
VITE_FACEBOOK_APP_ID=
